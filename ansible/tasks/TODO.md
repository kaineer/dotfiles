# Обновление podman-compose

## Установить pipx
sudo apt update && sudo apt install pipx -y

## Убедиться, что pipx в PATH
pipx ensurepath

## Установить podman-compose
pipx install podman-compose

## Проверить
podman-compose version
