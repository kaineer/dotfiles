return {
	name = "JSON",
	config = function(cmd)
		-- Not convenient at all
		-- cmd("FileType", {
		--   pattern = "json",
		--   callback = function()
		--     vim.cmd("Videre")
		--   end,
		-- })
	end,
}
