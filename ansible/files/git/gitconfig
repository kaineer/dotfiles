# User configuration
#
[user]
  email = kaineer@gmail.com
  name = Sergey Kluchkovsky

# User aliases
#
[alias]
  co = checkout
  st = status
  ci = commit
  lg = "log --color --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit --"
  wa = worktree add
  wl = worktree list
  wr = worktree remove


[pull]
  rebase = false

# Core user configuration
#
[core]
  excludesFile = ~/.gitignore
  editor = vi

[delta]
  navigate = true
  side-by-side = true
  syntax-theme = Nord

[diff]
  colorMoved = default

# Yep, still master, not main
#
[init]
  defaultBranch = master
[github]
	user = kaineer
