#!/usr/bin/env bash
#  _____  ___
# /__   \/ __\  Tangerine Cat, kaineer@gmail.com
#   / /\/ /
#  / / / /___   github: https://github.com/kaineer
#  \/  \____/   twitter: https://twitter.com/kaineer
#
# @link: https://github.com/clvv/fasd

### NOTE: this line means we should not load fasd
###   for non-interactive shell
[[ "$1" != "-i" ]] && return

eval "$(fasd --init posix-hook bash-hook)"

# Самостоятельное определение fasd_cd
fasd_cd() {
  [ $# -gt 1 ] && cd "$(fasd -e echo "$@")" || fasd "$@"
}

j() {
  BASE=$(basename $1)
  tmux rename-window "$BASE"

  fasd_cd -d $1
}

jg() {
  TARGET=$(fasd -ld | fzf)
  [[ "$TARGET" != "" ]] && cd $TARGET
}
