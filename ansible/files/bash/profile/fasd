#!/usr/bin/env bash

# @link: https://github.com/clvv/fasd

[[ "$1" != "-i" ]] && return

eval "$(fasd --init bash-hook)"

alias j='fasd_cd -d'

jj() {
  TARGET=$(fasd -l | fzf)
  [[ "$TARGET" != "" ]] && cd $TARGET
}
